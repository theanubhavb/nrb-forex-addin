"use strict";
var __importDefault = (this && this.__importDefault) || function (mod) {
    return (mod && mod.__esModule) ? mod : { "default": mod };
};
const customfunctionsplugin_1 = __importDefault(require("./customfunctionsplugin"));
// Add associate calls to the functions file's source (one file)
function addFunctionAssociations(source) {
    const input = this.getOptions().input;
    const file = this.getOptions().file;
    if (!customfunctionsplugin_1.default.generateResults[input]) {
        return source;
    }
    const associations = customfunctionsplugin_1.default.generateResults[input].associate.filter((item) => item.sourceFileName === file);
    associations.forEach((item) => {
        source += `\nCustomFunctions.associate("${item.id}", ${item.functionName});`;
    });
    return source;
}
module.exports = addFunctionAssociations;
//# sourceMappingURL=loader.js.map